name: ${PROJECT}
description: ${DESCRIPTION}
runtime: nodejs
template:
  description: A minimal AWS TypeScript Pulumi program
  important: true
  config:
    aws:region:
      description: The AWS region to deploy into
      default: us-east-1
    pulumiStackTeam:
      description: Pulumi RBAC Team to give admin access to
      default: DevTeam